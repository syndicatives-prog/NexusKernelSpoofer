name: Build NexusKernelSpoofer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
      - name: Build Driver
        run: msbuild driver/NexusSpoofer.sln /p:Configuration=Release /p:Platform=x64
